- title t(".search_team")

h4 = t(".teams_found", n: @teams.size, term: @term)

- if @teams.blank?
  = t("no_results")
  
- else
  table class="table table-striped table-condensed table-search"
    thead
      tr
        th = t(".name")
        th
      
    tbody
      = render partial: "search/team", collection: @teams