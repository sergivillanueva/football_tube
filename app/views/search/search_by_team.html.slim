- title t("matches_by", name: @term)
- breadcrumb :search_by_player

.m-b-lg
  h4 = t("search.results", n: @matches.size, term: @term)
- # TODO search by player name: use different search results views?

table class="table table-striped table-condensed table-search"
  thead
    tr
      th = t(".playing_date")
      th = t(".match")
      th = t(".competition")
      th = t(".season")
      th = t(".language")
      
  tbody
    = render partial: "search/match", collection: @matches